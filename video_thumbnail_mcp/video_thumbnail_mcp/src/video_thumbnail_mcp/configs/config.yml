general:
  use_uvloop: true
  logging:
    console:
      _type: console
      level: WARN

  front_end:
    _type: fastapi

  front_end:
    _type: console

workflow:
  _type: video_thumbnail_mcp
  parameter: default_value